# 项目结构说明

## 📁 完整目录树

```
show5/
├── 📄 README.md                    # 项目介绍和快速开始
├── 📄 QUICK_START.md               # 5分钟快速入门指南
├── 📄 CHANGELOG.md                 # 版本更新日志
├── 📄 PROJECT_STRUCTURE.md         # 本文件（项目结构说明）
├── 📄 prd.md                       # 产品需求文档（PRD）
├── 📄 requirements.txt             # Python 依赖列表
├── 📄 .gitignore                   # Git 忽略文件配置
├── 📄 .env.example                 # 环境变量示例文件
├── 📄 .env                         # 环境变量配置（需手动配置）
│
├── 📂 app/                         # 主应用目录
│   ├── 📄 __init__.py
│   ├── 📄 main.py                 # FastAPI 应用入口
│   ├── 📄 config.py               # 配置管理（Settings）
│   ├── 📄 logger.py               # 日志配置（Loguru）
│   │
│   ├── 📂 models/                 # 数据模型
│   │   ├── 📄 __init__.py
│   │   ├── 📄 database.py        # SQLAlchemy ORM 模型
│   │   └── 📄 schemas.py         # Pydantic 请求/响应模型
│   │
│   ├── 📂 db/                     # 数据库操作
│   │   ├── 📄 __init__.py
│   │   ├── 📄 database.py        # 数据库连接和会话管理
│   │   ├── 📄 crud.py            # CRUD 操作函数
│   │   └── 📄 init_db.py         # 数据库初始化脚本
│   │
│   ├── 📂 judges/                 # 评委系统核心模块
│   │   ├── 📄 __init__.py
│   │   ├── 📄 prompts.py         # Prompt 模板和评分规范
│   │   ├── 📄 utils.py           # 工具函数（客户端创建等）
│   │   ├── 📄 stage_one.py       # 阶段一：多评委并发评分
│   │   └── 📄 stage_two.py       # 阶段二：评委群聊讨论
│   │
│   └── 📂 api/                    # API 路由
│       ├── 📄 __init__.py
│       └── 📄 routes.py          # 路由定义和处理函数
│
├── 📂 docs/                       # 文档目录
│   ├── 📄 API.md                 # API 接口文档
│   ├── 📄 ARCHITECTURE.md        # 系统架构文档
│   └── 📄 DEPLOYMENT.md          # 部署指南
│
├── 📂 tests/                      # 测试目录
│   ├── 📄 __init__.py
│   ├── 📄 test_example.py        # 测试示例（Python）
│   └── 📄 test_api.sh            # API 测试脚本（Shell）
│
├── 📂 examples/                   # 示例代码
│   └── 📄 quick_start.py         # 快速开始示例
│
└── 📂 logs/                       # 日志目录（运行时自动创建）
    ├── 📄 ai_judge_YYYY-MM-DD.log       # 应用日志
    └── 📄 ai_judge_error_YYYY-MM-DD.log # 错误日志
```

## 📦 核心模块说明

### 1️⃣ 应用入口 (app/)

| 文件 | 功能 | 关键类/函数 |
|------|------|------------|
| `main.py` | FastAPI 应用主文件 | `app`, `lifespan()` |
| `config.py` | 配置管理 | `Settings`, `get_settings()` |
| `logger.py` | 日志配置 | `setup_logger()` |

### 2️⃣ 数据模型 (app/models/)

| 文件 | 功能 | 关键类 |
|------|------|--------|
| `database.py` | ORM 数据库模型 | `Entry`, `JudgeResult`, `DebateSession`, `DebateMessage` |
| `schemas.py` | API 请求/响应模型 | `JudgeEntryRequest`, `JudgeEntryResponse`, `EntryResponse` |

**表关系**：
```
Entry (作品)
  ├─ 1:N → JudgeResult (评分)
  └─ 1:N → DebateSession (讨论)
                └─ 1:N → DebateMessage (消息)
```

### 3️⃣ 数据库操作 (app/db/)

| 文件 | 功能 | 关键函数 |
|------|------|---------|
| `database.py` | 数据库连接 | `get_db()`, `init_database()` |
| `crud.py` | CRUD 操作 | `save_entry()`, `save_judge_results()`, `get_entry_by_id()` |
| `init_db.py` | 初始化脚本 | `main()` |

### 4️⃣ 评委系统 (app/judges/)

| 文件 | 功能 | 关键函数/常量 |
|------|------|--------------|
| `prompts.py` | Prompt 管理 | `COMMON_SCORING_GUIDE`, `JUDGE_PERSONAS`, `DEBATE_MODE_INSTRUCTION` |
| `utils.py` | 工具函数 | `make_vision_client()`, `parse_json_from_response()` |
| `stage_one.py` | 阶段一评分 | `score_image_with_all_judges()`, `build_vision_judges()` |
| `stage_two.py` | 阶段二讨论 | `run_debate_for_entry()`, `build_debate_judges()` |

**评委配置**：
- ChatGPT-5 评委：中立、理性、专业
- Grok 评委：幽默、犀利、直率
- Gemini 2.5 评委：细致、分析型、学院派
- 豆包评委：亲切、生活化、接地气
- 千问评委：文艺、感性、有情怀

### 5️⃣ API 路由 (app/api/)

| 文件 | 功能 | 路由 |
|------|------|------|
| `routes.py` | API 端点定义 | `POST /api/judge_entry`<br>`GET /api/judge_entry/{id}`<br>`GET /api/health` |

## 🔄 数据流程图

```
┌──────────────────────────────────────────────────────────────┐
│                         客户端请求                             │
│        POST /api/judge_entry (entry_id, image_url)           │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│              API Layer (app/api/routes.py)                    │
│  1. 验证请求参数 (Pydantic)                                     │
│  2. 保存作品信息到数据库                                        │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│          阶段一：评分 (app/judges/stage_one.py)                │
│  1. 下载图片 (requests + PIL)                                 │
│  2. 构建多模态消息 (MultiModalMessage)                         │
│  3. 并发调用 5 个评委 (asyncio.gather)                        │
│  4. 解析 JSON 响应                                            │
│  5. 排序结果                                                  │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│             保存评分结果 (app/db/crud.py)                      │
│  save_judge_results() → JudgeResult 表                        │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│          阶段二：讨论 (app/judges/stage_two.py)                │
│  1. 生成初评摘要上下文                                         │
│  2. 创建 SelectorGroupChat                                    │
│  3. 运行群聊讨论 (8-12 轮)                                     │
│  4. 收集讨论消息                                              │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│           保存讨论记录 (app/db/crud.py)                        │
│  save_debate_session() → DebateSession + DebateMessage 表     │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│                     返回完整结果                               │
│  {judge_results, sorted_results, debate}                      │
└──────────────────────────────────────────────────────────────┘
```

## 🛠️ 配置文件

### .env（环境变量）

```env
LLM_GATEWAY_BASE_URL=...    # LLM API 网关地址
LLM_GATEWAY_API_KEY=...     # API Key
MODEL_CHATGPT5=gpt-4o       # 各评委使用的模型
MODEL_GROK=grok-beta
MODEL_GEMINI=gemini-2.0-flash-exp
MODEL_DOUBAO=doubao-pro-32k
MODEL_QWEN=qwen-max
MODEL_SELECTOR=gpt-4o-mini  # 选择器模型
DATABASE_URL=...            # 数据库连接字符串
SERVER_HOST=0.0.0.0         # 服务器地址
SERVER_PORT=8000            # 服务器端口
MAX_DEBATE_MESSAGES=12      # 讨论最大轮数
```

## 📚 文档索引

| 文档 | 内容 |
|------|------|
| `README.md` | 项目介绍、特性、快速开始 |
| `QUICK_START.md` | 5 分钟快速入门指南 |
| `docs/API.md` | API 接口详细文档 |
| `docs/ARCHITECTURE.md` | 系统架构和技术选型 |
| `docs/DEPLOYMENT.md` | 部署指南（Docker、Systemd 等）|
| `CHANGELOG.md` | 版本更新历史 |
| `prd.md` | 产品需求文档（原始设计）|

## 🧪 测试文件

| 文件 | 用途 |
|------|------|
| `tests/test_example.py` | Python 测试脚本（测试阶段一和阶段二）|
| `tests/test_api.sh` | Shell 脚本（测试 API 接口）|
| `examples/quick_start.py` | 快速开始示例代码 |

## 🚀 启动流程

1. **安装依赖**：`pip install -r requirements.txt`
2. **配置环境**：编辑 `.env` 文件
3. **初始化数据库**：`python -m app.db.init_db`
4. **启动服务**：`python app/main.py` 或 `uvicorn app.main:app --reload`
5. **访问文档**：http://localhost:8000/docs

## 📊 数据库表结构

### entries（作品表）
- `entry_id` (PK): 作品 ID
- `image_url`: 图片 URL
- `competition_type`: 比赛类型
- `extra_text`: 补充说明
- `created_at`, `updated_at`: 时间戳

### judge_results（评分表）
- `id` (PK): 自增 ID
- `entry_id` (FK): 关联作品
- `judge_id`: 评委 ID
- `judge_display_name`: 评委显示名
- `overall_score`: 总分
- `dimension_scores`: 维度评分 (JSON)
- `strengths`: 优点 (JSON)
- `weaknesses`: 缺点 (JSON)
- `one_liner`: 一句话点评
- `comment_for_audience`: 详细评语
- `safety_notes`: 安全提示 (JSON)
- `raw_output`: 原始输出
- `created_at`: 创建时间

### debate_sessions（讨论会话表）
- `debate_id` (PK): 会话 ID
- `entry_id` (FK): 关联作品
- `participants`: 参与者列表 (JSON)
- `config`: 配置 (JSON)
- `created_at`: 创建时间

### debate_messages（讨论消息表）
- `id` (PK): 自增 ID
- `debate_id` (FK): 关联会话
- `sequence`: 消息序号
- `speaker`: 发言者
- `content`: 发言内容
- `created_at`: 创建时间

## 🔑 关键依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| autogen-agentchat | 0.4.0.dev8 | 多 Agent 协作框架 |
| autogen-ext[openai] | 0.4.0.dev8 | OpenAI 扩展 |
| fastapi | 0.115.0 | Web 框架 |
| uvicorn | 0.32.0 | ASGI 服务器 |
| sqlalchemy | 2.0.35 | ORM |
| pydantic | 2.9.2 | 数据验证 |
| loguru | 0.7.2 | 日志管理 |
| Pillow | 10.4.0 | 图像处理 |

## 💡 扩展指南

### 添加新评委
编辑 `app/judges/prompts.py`，在 `JUDGE_PERSONAS` 中添加新评委配置。

### 添加新比赛类型
在 `COMMON_SCORING_GUIDE` 中添加新的维度定义。

### 自定义评分维度
修改 Prompt 中的维度描述，评委会自动适配。

### 异步任务化
集成 Celery，将评分流程改为异步任务。

---

**项目完整实现，开箱即用！** 🎉

