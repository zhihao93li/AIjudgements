# AI Judge System - AI 评委评分系统

基于多模态 AI 的娱乐评分系统，支持穿搭大赛、搞笑图片大赛等多种比赛类型。

## 特性

- 🎭 **多评委系统**：ChatGPT-5、Grok、Gemini 2.5、豆包、千问等多个模型扮演不同人设
- 👁️ **多模态支持**：评委直接看图评分，无需文字描述
- 💬 **智能群聊**：基于 AutoGen 的 SelectorGroupChat，评委自动讨论互动
- 📊 **结构化评分**：统一 JSON 输出格式，包含总分、维度分、优缺点等
- 🚀 **高性能**：并发评分，快速响应

## 系统架构

### 两阶段评分流程

**阶段一：独立评分**
- 多个评委并发观看参赛图片
- 每个评委输出结构化 JSON 评分
- 自动排序并生成排行榜

**阶段二：群聊讨论**
- 基于阶段一评分结果
- 评委们进行互动讨论
- 产出综艺感对话内容

## 快速开始

### 前置要求

- **Python 3.10 或更高版本**（推荐 Python 3.11）
- 根据 [AutoGen 官方文档](https://microsoft.github.io/autogen/stable/)，最新版本需要 Python 3.10+

> 📝 如果你的 Python 版本低于 3.10，请查看 [INSTALL_PYTHON.md](INSTALL_PYTHON.md) 了解如何安装 Python 3.11

### 方法 1：自动化安装（推荐）

```bash
# 运行自动化设置脚本
./setup.sh
```

这个脚本会自动：
1. ✅ 检查 Python 版本
2. ✅ 创建虚拟环境
3. ✅ 安装所有依赖
4. ✅ 初始化数据库
5. ✅ 验证安装

### 方法 2：手动安装

#### 1. 安装 Python 3.11

如果还没有 Python 3.11，访问：https://www.python.org/downloads/macos/

#### 2. 创建虚拟环境

```bash
python3.11 -m venv venv
source venv/bin/activate  # Mac/Linux
# 或 venv\Scripts\activate  # Windows
```

#### 3. 安装依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

#### 4. 配置环境变量

```bash
# 编辑 .env 文件，填入你的 API Key
nano .env  # 或使用其他编辑器
```

#### 5. 初始化数据库

```bash
python -m app.db.init_db
```

#### 6. 启动服务

```bash
python app/main.py
# 或
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 5. 测试 API

```bash
curl -X POST "http://localhost:8000/api/judge_entry" \
  -H "Content-Type: application/json" \
  -d '{
    "entry_id": "entry_001",
    "image_url": "https://example.com/your-image.jpg",
    "competition_type": "outfit",
    "extra_text": "日常通勤穿搭"
  }'
```

## API 文档

启动服务后访问：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 主要接口

#### POST /api/judge_entry
提交参赛作品并触发评分

**请求体：**
```json
{
  "entry_id": "entry_001",
  "image_url": "https://example.com/image.jpg",
  "competition_type": "outfit",
  "extra_text": "补充说明（可选）"
}
```

#### GET /api/judge_entry/{entry_id}
查询某个作品的评分和讨论记录

## 项目结构

```
show5/
├── app/
│   ├── main.py              # FastAPI 主应用
│   ├── config.py            # 配置管理
│   ├── models/              # 数据模型
│   │   ├── database.py      # SQLAlchemy 模型
│   │   └── schemas.py       # Pydantic 模型
│   ├── judges/              # 评委系统
│   │   ├── stage_one.py     # 阶段一：评分
│   │   ├── stage_two.py     # 阶段二：讨论
│   │   ├── prompts.py       # Prompt 模板
│   │   └── utils.py         # 工具函数
│   ├── api/                 # API 路由
│   │   └── routes.py
│   └── db/                  # 数据库操作
│       ├── init_db.py
│       └── crud.py
├── tests/                   # 测试文件
├── logs/                    # 日志目录
├── requirements.txt         # 依赖
├── .env.example            # 环境变量示例
└── README.md               # 本文件
```

## 支持的比赛类型

- `outfit` - 穿搭大赛
- `funny` - 搞笑图片大赛
- 可扩展自定义类型

## 技术栈

- **AutoGen**: 多 Agent 协作框架
- **FastAPI**: 高性能 Web 框架
- **SQLAlchemy**: ORM 数据库操作
- **Pillow**: 图像处理
- **Loguru**: 日志管理

## 开发计划

- [x] v1: 单图评分 + 群聊讨论
- [ ] v2: 多图支持
- [ ] v3: 主持人角色
- [ ] v4: 异步任务队列

## License

MIT




