# 快速开始指南

## 🚀 5 分钟快速体验

### 步骤 1：安装依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv
source venv/bin/activate  # Mac/Linux
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 步骤 2：配置 API Key

编辑 `.env` 文件（已创建），填入你的 API Key：

```bash
LLM_GATEWAY_API_KEY=your-api-key-here
```

> 💡 如果你使用的是 OpenAI 官方 API，保持 `LLM_GATEWAY_BASE_URL=https://api.openai.com/v1` 即可。
> 
> 如果使用其他网关（如 one-api），请修改对应的 base_url。

### 步骤 3：初始化数据库

```bash
python -m app.db.init_db
```

### 步骤 4：启动服务

```bash
# 方式 1：直接运行
python app/main.py

# 方式 2：使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

看到以下输出说明启动成功：

```
============================================================
AI Judge System 正在启动...
============================================================
数据库初始化完成
系统启动完成！
API 文档地址: http://0.0.0.0:8000/docs
```

### 步骤 5：测试 API

#### 方式 1：使用 Swagger UI（推荐）

打开浏览器访问：http://localhost:8000/docs

在 Swagger UI 中：
1. 点击 **POST /api/judge_entry**
2. 点击 **Try it out**
3. 填入测试数据：

```json
{
  "entry_id": "test_001",
  "image_url": "https://picsum.photos/800/600",
  "competition_type": "outfit",
  "extra_text": "测试穿搭图片"
}
```

4. 点击 **Execute**
5. 等待 30-60 秒（取决于模型响应速度）
6. 查看结果！

#### 方式 2：使用 cURL

```bash
curl -X POST "http://localhost:8000/api/judge_entry" \
  -H "Content-Type: application/json" \
  -d '{
    "entry_id": "test_001",
    "image_url": "https://picsum.photos/800/600",
    "competition_type": "outfit",
    "extra_text": "测试穿搭图片"
  }'
```

#### 方式 3：使用 Python 脚本

```bash
python examples/quick_start.py
```

## 📊 理解返回结果

成功后会返回类似这样的 JSON：

```json
{
  "entry_id": "test_001",
  "competition_type": "outfit",
  "judge_results": [
    {
      "judge_id": "chatgpt5_judge",
      "judge_display_name": "ChatGPT-5 评委",
      "overall_score": 8.5,
      "dimension_scores": [...],
      "strengths": ["色彩搭配好", "风格统一"],
      "weaknesses": ["配饰略简单"],
      "one_liner": "整体不错，有提升空间",
      "comment_for_audience": "这是一套很适合..."
    },
    // ... 其他 4 个评委
  ],
  "sorted_results": [
    // 按分数排序的结果
  ],
  "debate": {
    "debate_id": "test_001_debate",
    "participants": ["chatgpt5_judge", "grok_judge", ...],
    "messages": [
      {
        "sequence": 1,
        "speaker": "grok_judge",
        "content": "我觉得这个搭配挺有意思的..."
      },
      {
        "sequence": 2,
        "speaker": "chatgpt5_judge",
        "content": "我同意 Grok 的观点，但是..."
      }
      // ... 更多讨论消息
    ]
  }
}
```

## 🎯 关键字段说明

| 字段 | 说明 |
|------|------|
| `overall_score` | 总分（0-10 分）|
| `dimension_scores` | 各维度详细评分 |
| `strengths` | 优点列表 |
| `weaknesses` | 缺点列表 |
| `one_liner` | 一句话点评 |
| `sorted_results` | 按分数排序的评委列表（排行榜）|
| `debate.messages` | 评委群聊记录 |

## 🔧 常见问题

### Q1: API Key 无效？

**解决**：
- 检查 `.env` 文件中的 `LLM_GATEWAY_API_KEY` 是否正确
- 确认 API Key 有足够的额度
- 确认模型名称配置正确（如 `gpt-4o`）

### Q2: 图片下载失败？

**解决**：
- 确保 `image_url` 是公网可访问的
- 测试：在浏览器中打开该 URL，看能否显示图片
- 检查服务器网络是否正常

### Q3: 某些评委评分失败？

**解决**：
- 查看日志：`tail -f logs/ai_judge_*.log`
- 可能是模型名称配置错误
- 可能是该模型暂时不可用
- 系统会容错处理，不影响其他评委

### Q4: 响应时间太长？

**正常情况**：
- 阶段一（评分）：20-40 秒（5 个评委并发）
- 阶段二（讨论）：30-60 秒（10-12 轮对话）
- 总计：50-100 秒

**优化方案**：
- 减少评委数量
- 使用更快的模型（如 gpt-4o-mini）
- 减少讨论轮数（修改 `MAX_DEBATE_MESSAGES`）

### Q5: 内存占用高？

**解决**：
- 正常情况下约占用 200-500MB
- 如果过高，检查是否有未释放的资源
- 考虑使用更轻量的模型

## 📖 下一步

- 📚 阅读完整 [API 文档](docs/API.md)
- 🏗️ 了解[系统架构](docs/ARCHITECTURE.md)
- 🚢 查看[部署指南](docs/DEPLOYMENT.md)
- 💡 修改 `app/judges/prompts.py` 自定义评委人设
- 🎨 添加自己的比赛类型和评分维度

## 🎉 开始创作！

现在你可以：

1. 上传各种创意图片让 AI 评委评分
2. 观察不同评委的风格差异
3. 欣赏评委们的精彩讨论
4. 将结果用于内容创作、综艺节目、社交媒体等

**玩得开心！** 🚀

